#!/bin/bash
#
# test simple command "ls" with relly a lot spaces before the command, trying to find if it ever fails, we leave almost three lines of only spaces
command="                                                                                                        ls"
# output:  lists directory contents of files and directories
